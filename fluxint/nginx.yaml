apiVersion: v1
kind: Pod
metadata:
  name: nginx
  labels:
    env: test
spec:
  containers:
  - name: bitnami-nginx
    image: bitnami/nginx
    imagePullPolicy: IfNotPresent
  nodeSelector:
    disktype: ssd
